---
import ToolLayout from '../../layouts/ToolLayout.astro';
---

<ToolLayout
  title="URL Encoder / Decoder"
  description="Free online URL encoder and decoder — encode and decode URL strings instantly. Fast, client-side URL encoding tool."
  keywords="url encoder, url decoder, url encode, url decode, url encoding online, url decoding tool, free url encoder, percent encoding"
>
  <div class="tool-header-row">
    <label for="input" class="sr-only">URL input</label>
    <textarea id="input" class="input-area monospace" placeholder="Enter text or URL to encode/decode..." spellcheck="false" autofocus></textarea>
    <button class="btn-secondary-sm try-sample" id="btn-sample">Try sample</button>
  </div>

  <div class="button-row">
    <button class="btn-primary" id="btn-encode">Encode</button>
    <button class="btn-secondary" id="btn-decode">Decode</button>
    <button class="btn-secondary" id="btn-copy">Copy</button>
    <button class="btn-secondary" id="btn-clear">Clear</button>
    <span class="shortcut-hint" aria-live="polite">Ctrl+Enter to encode</span>
  </div>

  <div class="options-row">
    <label class="checkbox-label">
      <input type="checkbox" id="full-encode" class="checkbox-input" />
      Fully encode (including query params & /)
    </label>
  </div>

  <div id="error" class="error-msg" role="alert" hidden></div>

  <div class="output-area">
    <pre id="output" class="output-content" aria-label="Result" data-placeholder="Encoded or decoded result will appear here..."></pre>
  </div>
</ToolLayout>

<script>
  const input = document.getElementById('input') as HTMLTextAreaElement;
  const output = document.getElementById('output') as HTMLPreElement;
  const errorEl = document.getElementById('error') as HTMLDivElement;
  const copyBtn = document.getElementById('btn-copy') as HTMLButtonElement;
  const fullEncode = document.getElementById('full-encode') as HTMLInputElement;

  function doEncode() {
    const raw = input.value;
    if (!raw) { errorEl.textContent = 'Please enter text to encode.'; errorEl.hidden = false; output.textContent = ''; return; }
    errorEl.hidden = true;
    try {
      output.textContent = fullEncode.checked ? encodeURIComponent(raw) : encodeURI(raw);
    } catch { errorEl.textContent = 'Failed to encode.'; errorEl.hidden = false; }
  }

  function doDecode() {
    const raw = input.value;
    if (!raw) { errorEl.textContent = 'Please enter text to decode.'; errorEl.hidden = false; output.textContent = ''; return; }
    errorEl.hidden = true;
    try {
      output.textContent = decodeURIComponent(raw.replace(/\+/g, ' '));
    } catch { errorEl.textContent = 'Invalid URL encoding.'; errorEl.hidden = false; }
  }

  function copyOutput() {
    const text = output.textContent;
    if (!text) return;
    navigator.clipboard.writeText(text).then(() => {
      const orig = copyBtn.textContent || '';
      copyBtn.textContent = 'Copied \u2713';
      copyBtn.classList.add('btn-copied');
      setTimeout(() => { copyBtn.textContent = orig; copyBtn.classList.remove('btn-copied'); }, 1500);
    }).catch(() => {});
  }

  function clearAll() {
    input.value = '';
    output.textContent = '';
    errorEl.hidden = true;
  }

  function loadSample() {
    input.value = 'https://example.com/search?q=hello world&category=dev tools';
    output.textContent = '';
    errorEl.hidden = true;
  }

  function handleKeydown(e: KeyboardEvent) {
    if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') { e.preventDefault(); doEncode(); }
    if (e.key === 'Escape') { clearAll(); }
  }

  function mount() {
    input.addEventListener('keydown', handleKeydown);
    document.getElementById('btn-encode')!.addEventListener('click', doEncode);
    document.getElementById('btn-decode')!.addEventListener('click', doDecode);
    document.getElementById('btn-copy')!.addEventListener('click', copyOutput);
    document.getElementById('btn-clear')!.addEventListener('click', clearAll);
    document.getElementById('btn-sample')!.addEventListener('click', loadSample);
  }

  mount();
  document.addEventListener('astro:before-swap', () => input.removeEventListener('keydown', handleKeydown));
</script>

<style>
  .tool-header-row { position: relative; }
  .try-sample { position: absolute; top: var(--spacing-sm); right: var(--spacing-sm); z-index: 1; }
  .input-area { width: 100%; min-height: 200px; padding: var(--spacing-md); font-size: var(--font-size-code); line-height: 1.5; background-color: var(--color-canvas); color: var(--color-ink); border: 1px solid var(--color-hairline); border-radius: var(--border-radius-sm); resize: vertical; box-sizing: border-box; }
  .input-area.monospace { font-family: var(--font-geist-mono); }
  .input-area:focus { outline: 2px solid var(--color-ink); outline-offset: 2px; }
  .input-area::placeholder { color: var(--color-mute); }
  .button-row { display: flex; gap: var(--spacing-sm); margin: var(--spacing-md) 0; align-items: center; flex-wrap: wrap; }
  .shortcut-hint { font-family: var(--font-geist-mono); font-size: var(--font-size-caption-mono); color: var(--color-mute); }
  @media (min-width: 640px) { .shortcut-hint { margin-left: auto; } }
  .options-row { margin-bottom: var(--spacing-md); }
  .checkbox-label { display: inline-flex; align-items: center; gap: var(--spacing-xs); font-size: var(--font-size-body-sm); color: var(--color-body); cursor: pointer; }
  .checkbox-input { width: 16px; height: 16px; accent-color: var(--color-primary); cursor: pointer; }
  .error-msg { font-family: var(--font-geist-mono); font-size: var(--font-size-body-sm); color: var(--color-error); background-color: var(--color-error-soft); padding: var(--spacing-sm) var(--spacing-md); border-radius: var(--border-radius-sm); margin-bottom: var(--spacing-md); }
  .output-area { background-color: var(--color-canvas-soft-2); border-radius: var(--border-radius-md); border: 1px solid #00000014; padding: 1rem; min-height: 200px; max-height: 600px; overflow-y: auto; }
  .output-content { font-family: var(--font-geist-mono); font-size: var(--font-size-code); line-height: 1.5; color: var(--color-ink); margin: 0; white-space: pre-wrap; word-break: break-all; }
</style>
